{
  "name": "pnpm-deploy-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -r --stream --parallel run dev",
    "build": "pnpm -r run build",
    "deploy": "pnpm --filter=my-app --prod deploy dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^4.1.2",
    "tsup": "^6.6.3",
    "typescript": "^4.9.5"
  }
}
